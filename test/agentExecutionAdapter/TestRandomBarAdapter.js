describe('RandomBarAdapter', function () {
  fit("can create histo data for testing", function () {
    var rba = new RandomBarAdapter("EUR_CHF", "D1", "01/01/2010");
    rba.init();






    //constructor(symbol, period, currentDate, options = {}) {
    //  super(symbol, period, currentDate);
    //  this.seed = option.seed || 1;
    //  this.nbInitialTicksByPeriod = option.nbInitialTicksByPeriod || 10;
    //  this.initialValue = option.initialValue || 1;
    //  this.maxVolumeByTick = option.maxVolumeByTick || 10000;
    //  this.returnsByTick = option.returnsByTick || 0.001;
    //  this.roundTo = Math.round(Math.pow(10, -Math.log(option.roundTo || 0.0001) / Math.log(10)));
    //}
  });

});